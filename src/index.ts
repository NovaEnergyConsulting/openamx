/**
 * OpenAMX public API entry point.
 * 
 * This module will eventually re-export the main parser, evaluator,
 * renderer, and CLI entry points.
 * 
 * Currently a placeholder for Sprint 001 scaffolding.
 */

// Placeholder export to ensure the module compiles
export const OPENAMX_VERSION = "0.1.0" as const;
